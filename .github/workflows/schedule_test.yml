name: Schedule Test

on:
  schedule:
    - cron: "15,30,45 * * * *"

jobs:
  build:
    runs-on: windows-latest
    steps:
    
      - name: Run a script
        run: echo "This workflow runs on a schedule!"
